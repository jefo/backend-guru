---
# === Основная информация ===
title: "Божественный объект (God Object)"
description: "Божественный объект — это класс, который знает слишком много или делает слишком много. Он нарушает принцип единственной ответственности и становится центральной точкой контроля для множества различных операций."

# === Таксономия и Навигация ===
section: "smells"
category: "complexity"
severity: "high"

# === Визуал ===
cover: "/images/smells/god-object-cover.svg"

# === Связи ===
related:
  - "anemic-model"
solutions:
  - "aggregate"
  - "entity"
  - "value-object"

# === Метаданные ===
published: true
lastUpdated: "2024-05-21"
---

# Божественный объект (God Object)

### 🤕 Проблема

В процессе разработки один класс постепенно накапливает все больше и больше ответственностей, становясь центральной точкой управления для множества различных операций. Этот класс знает обо всем в системе, контролирует множество различных процессов и содержит тысячи строк кода.

Такой "божественный объект" становится узким местом разработки: любое изменение затрагивает его, он сложен для понимания, тестирования и сопровождения. Разработчики боятся его трогать, что приводит к еще большему накоплению кода в этом классе.

Типичные признаки божественного объекта:
- Сотни или тысячи строк кода
- Десятки методов с совершенно разной функциональностью
- Множество зависимостей от других классов
- Сложность понимания того, что делает класс
- Невозможность написать внятные unit-тесты

### 💡 Признаки и симптомы

#### 1. Размер и сложность
```typescript
// Пример God Object - класс на 2000+ строк
class OrderManager {
    // Управление заказами
    public createOrder(customerData: any, items: any[]): Order { ... }
    public updateOrderStatus(orderId: string, status: string): void { ... }
    public cancelOrder(orderId: string, reason: string): void { ... }
    
    // Управление клиентами
    public createCustomer(data: CustomerData): Customer { ... }
    public updateCustomerProfile(id: string, data: any): void { ... }
    public validateCustomerCreditLimit(customerId: string): boolean { ... }
    
    // Управление товарами
    public checkProductAvailability(productId: string): boolean { ... }
    public updateProductPrice(productId: string, newPrice: number): void { ... }
    public calculateProductDiscount(productId: string, customer: Customer): number { ... }
    
    // Управление платежами
    public processPayment(orderId: string, paymentData: any): PaymentResult { ... }
    public refundPayment(orderId: string, amount: number): void { ... }
    public validatePaymentMethod(method: PaymentMethod): boolean { ... }
    
    // Управление доставкой
    public calculateShippingCost(order: Order, address: Address): number { ... }
    public scheduleDelivery(orderId: string, date: Date): void { ... }
    public trackShipment(orderId: string): TrackingInfo { ... }
    
    // Отчеты и аналитика
    public generateSalesReport(period: DateRange): SalesReport { ... }
    public calculateCustomerLifetimeValue(customerId: string): number { ... }
    public getTopSellingProducts(period: DateRange): Product[] { ... }
    
    // Уведомления
    public sendOrderConfirmation(orderId: string): void { ... }
    public sendShippingNotification(orderId: string): void { ... }
    public sendPromotionalEmail(customerId: string, campaign: Campaign): void { ... }
    
    // И еще сотни других методов...
}
```

#### 2. Множественные ответственности
Божественный объект часто отвечает за:
- Бизнес-логику
- Валидацию данных
- Взаимодействие с базой данных
- Внешние API вызовы
- Логирование
- Кеширование
- Форматирование данных
- Уведомления

#### 3. Сложные зависимости
```typescript
class OrderManager {
    constructor(
        private database: DatabaseService,
        private paymentGateway: PaymentGateway,
        private emailService: EmailService,
        private smsService: SmsService,
        private inventoryService: InventoryService,
        private shippingService: ShippingService,
        private customerService: CustomerService,
        private productService: ProductService,
        private discountService: DiscountService,
        private taxService: TaxService,
        private auditService: AuditService,
        private analyticsService: AnalyticsService,
        private cacheService: CacheService,
        private configService: ConfigService
        // ... и еще десятки зависимостей
    ) {}
}
```

### 🔧 Решение: Разделение ответственностей

#### 1. Выделение доменных сущностей

Вместо одного большого класса создайте специализированные классы для каждой области:

```typescript
// Разделяем на специализированные классы

// Доменная сущность для заказов
class Order {
    private _id: OrderId;
    private _customerId: CustomerId;
    private _items: OrderItem[];
    private _status: OrderStatus;
    private _createdAt: Date;
    
    constructor(customerId: CustomerId, items: OrderItem[]) {
        this._id = OrderId.generate();
        this._customerId = customerId;
        this._items = [...items];
        this._status = OrderStatus.Pending;
        this._createdAt = new Date();
    }
    
    public addItem(item: OrderItem): void {
        if (this._status !== OrderStatus.Pending) {
            throw new Error('Cannot modify confirmed order');
        }
        this._items.push(item);
    }
    
    public confirm(): void {
        if (this._items.length === 0) {
            throw new Error('Cannot confirm empty order');
        }
        this._status = OrderStatus.Confirmed;
    }
    
    public calculateTotal(): Money {
        return this._items.reduce(
            (total, item) => total.add(item.subtotal()),
            Money.zero()
        );
    }
    
    // Только методы, связанные с заказом
}

// Доменная сущность для клиентов
class Customer {
    private _id: CustomerId;
    private _email: Email;
    private _profile: CustomerProfile;
    private _creditLimit: Money;
    
    constructor(email: Email, profile: CustomerProfile) {
        this._id = CustomerId.generate();
        this._email = email;
        this._profile = profile;
        this._creditLimit = Money.zero();
    }
    
    public canPurchase(amount: Money): boolean {
        return this._creditLimit.isGreaterOrEqual(amount);
    }
    
    public increaseCreditLimit(amount: Money): void {
        this._creditLimit = this._creditLimit.add(amount);
    }
    
    // Только методы, связанные с клиентом
}
```

#### 2. Создание специализированных сервисов

```typescript
// Сервис для работы с заказами
class OrderService {
    constructor(
        private orderRepository: OrderRepository,
        private inventoryService: InventoryService
    ) {}
    
    public async createOrder(customerId: CustomerId, items: OrderItem[]): Promise<Order> {
        // Проверяем доступность товаров
        for (const item of items) {
            const isAvailable = await this.inventoryService.checkAvailability(
                item.productId, 
                item.quantity
            );
            if (!isAvailable) {
                throw new Error(`Product ${item.productId} is not available`);
            }
        }
        
        const order = new Order(customerId, items);
        return await this.orderRepository.save(order);
    }
}

// Сервис для работы с платежами
class PaymentService {
    constructor(
        private paymentGateway: PaymentGateway,
        private paymentRepository: PaymentRepository
    ) {}
    
    public async processPayment(orderId: OrderId, paymentMethod: PaymentMethod): Promise<Payment> {
        const payment = new Payment(orderId, paymentMethod);
        
        try {
            const result = await this.paymentGateway.charge(
                paymentMethod,
                payment.amount
            );
            
            payment.markAsSuccessful(result.transactionId);
            return await this.paymentRepository.save(payment);
        } catch (error) {
            payment.markAsFailed(error.message);
            await this.paymentRepository.save(payment);
            throw error;
        }
    }
}

// Сервис для уведомлений
class NotificationService {
    constructor(
        private emailService: EmailService,
        private smsService: SmsService
    ) {}
    
    public async sendOrderConfirmation(order: Order, customer: Customer): Promise<void> {
        const emailTemplate = new OrderConfirmationEmail(order, customer);
        await this.emailService.send(customer.email, emailTemplate);
    }
}
```

#### 3. Координация через Application Services

```typescript
// Application Service координирует работу доменных сервисов
class OrderApplicationService {
    constructor(
        private orderService: OrderService,
        private paymentService: PaymentService,
        private notificationService: NotificationService,
        private customerRepository: CustomerRepository
    ) {}
    
    public async placeOrder(command: PlaceOrderCommand): Promise<OrderResult> {
        // 1. Найти клиента
        const customer = await this.customerRepository.findById(command.customerId);
        if (!customer) {
            throw new Error('Customer not found');
        }
        
        // 2. Создать заказ
        const order = await this.orderService.createOrder(
            command.customerId,
            command.items
        );
        
        // 3. Обработать платеж
        try {
            const payment = await this.paymentService.processPayment(
                order.id,
                command.paymentMethod
            );
            
            // 4. Подтвердить заказ
            order.confirm();
            
            // 5. Отправить уведомление
            await this.notificationService.sendOrderConfirmation(order, customer);
            
            return new OrderResult(order.id, payment.id);
        } catch (paymentError) {
            // Откатить заказ при ошибке платежа
            await this.orderService.cancelOrder(order.id);
            throw paymentError;
        }
    }
}
```

### ✅ Результат рефакторинга

После разбиения божественного объекта получаем:

#### Преимущества:
- **Ясность**: Каждый класс имеет четкую ответственность
- **Тестируемость**: Легко писать unit-тесты для каждого компонента
- **Повторное использование**: Сервисы можно использовать в разных контекстах
- **Независимое развитие**: Изменения в одной области не затрагивают другие
- **Читаемость**: Код стал понятнее и проще для понимания

#### Структура после рефакторинга:
```
Domain/
├── Entities/
│   ├── Order.ts
│   ├── Customer.ts
│   ├── Product.ts
│   └── Payment.ts
├── ValueObjects/
│   ├── OrderId.ts
│   ├── Money.ts
│   └── Email.ts
└── Services/
    ├── OrderService.ts
    ├── PaymentService.ts
    └── NotificationService.ts

Application/
└── Services/
    ├── OrderApplicationService.ts
    ├── CustomerApplicationService.ts
    └── ProductApplicationService.ts
```

### 🚨 Как избежать создания God Object

#### 1. Следуйте принципу единственной ответственности (SRP)
Каждый класс должен иметь только одну причину для изменения.

#### 2. Применяйте правило "Если класс делает X И Y, то разделите его"
Союз "И" в описании класса — признак множественной ответственности.

#### 3. Ограничивайте размер классов
- Не более 200-300 строк кода
- Не более 10-15 публичных методов
- Не более 7-10 зависимостей

#### 4. Регулярно проводите code review
Ищите классы, которые растут слишком быстро.

#### 5. Используйте метрики
Отслеживайте цикломатическую сложность, связанность и сцепление.

### 👍 Плюсы и 👎 Минусы рефакторинга

| Плюсы | Минусы |
|-------|--------|
| Улучшение читаемости и понимания кода | Увеличение количества классов и файлов |
| Упрощение тестирования отдельных компонентов | Необходимость координации между сервисами |
| Повышение гибкости и возможности изменений | Первоначальные затраты времени на рефакторинг |
| Снижение связанности между компонентами | Возможное увеличение сложности архитектуры |
| Улучшение повторного использования кода | Необходимость продуманного проектирования взаимодействий |

### 🎯 Практические шаги по устранению

1. **Анализ**: Определите все ответственности божественного объекта
2. **Группировка**: Сгруппируйте связанные методы и данные
3. **Выделение**: Создайте отдельные классы для каждой группы
4. **Тестирование**: Напишите тесты для новых классов
5. **Рефакторинг**: Постепенно переносите код из старого класса
6. **Интеграция**: Настройте взаимодействие между новыми компонентами
7. **Удаление**: Удалите старый божественный объект

Помните: божественный объект — это результат постепенного накопления кода. Предотвратить его появление проще, чем рефакторить потом.